name: Pipeline Setup Testing

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
    - name: Deploy to server 
      run: |
        ssh -i ${{ 157.245.86.84 }} ${{ root }} ${{ SSH_PRIVATE_KEY }} ${{ 22 }}
    - name: Deploy
      run:
        cd /root/mohid/invozone
        mkdir test
